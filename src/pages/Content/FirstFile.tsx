import { Typography } from '@mui/material'

const FirstFile = () => {
    return (
        <div>
            <Typography sx={{ color: 'white' }}>
                File Content
            </Typography>
        </div>
    )
}

export default FirstFile